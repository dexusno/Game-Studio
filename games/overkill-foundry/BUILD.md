# Overkill Foundry — build and run

Engine preference: unreal. No engine project has been created. There is no build or run command yet.

## Prerequisites

Record the exact engine/runtime version, supported host platforms, installed dependencies and external setup steps after implementation begins. Keep private machine paths in the root ignored config.local.json.

## Reproduction instructions

- Install/setup: not defined.
- Run in development: not defined.
- Build/package: not defined.
- Automated tests: not defined.
- Expected output location: not defined.

## Verification record

No build has been run. For each verified build, record date, commit, platform, exact command, result and artifact location. Explain failures and manual dependencies.
